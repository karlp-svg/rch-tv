name = "rch-tv"
compatibility_date = "2024-01-01"

[build]
command = "npm run build"
publish = ".next"

# Environment variables (set these in Cloudflare dashboard, not here)
# These are just placeholders for local testing
[vars]
NODE_ENV = "production"